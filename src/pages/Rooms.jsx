// src/pages/Rooms.js
import React from "react";
import { Typography } from "@mui/material";

const Rooms = () => {
  return (
    <div>
      <Typography variant="h4">Rooms</Typography>
      {/* Future: Fetch and display room data from the database */}
    </div>
  );
};

export default Rooms;
