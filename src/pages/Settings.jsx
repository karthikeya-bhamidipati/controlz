// src/pages/Settings.js
import React from "react";
import { Typography } from "@mui/material";

const Settings = () => {
  return (
    <div>
      <Typography variant="h4">Settings</Typography>
      {/* Future: Fetch and display user settings from the database */}
    </div>
  );
};

export default Settings;
